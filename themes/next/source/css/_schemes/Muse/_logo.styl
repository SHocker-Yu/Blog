.custom-logo {
  .site-meta-headline { text-align: center; }

  .brand { background: none; }

  .site-title {
    
  
  font-style: normal;
    margin: 0 auto;
    font-size: 26px;
    font-weight: bold;
    line-height: 50px;
    letter-spacing: -0.03em;
    height: 50px;
    color: $black-deep;
    a { border: none; }
  }


}

.custom-logo-image {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  -o-object-fit: cover;
  object-fit: cover;
  overflow: hidden;
}

.under-subtitle-line {
  max-width: 400px;
  height: 1px;
  margin-top: -1px;
  border: none;
}
